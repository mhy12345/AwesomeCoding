// for 本地 npm run server 测试，注意要开启 allow control 插件
// var host = require('../../backend/configures/server_config');
// var root_url = 'http://' + host.ip + ':' + host.port;
// module.exports = root_url;

// todo   for 最终发布版本/npm run build 测试
var root_url = '';
module.exports = root_url;
